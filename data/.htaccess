# Pending (un-moderated) reviews live here. They must NEVER be web-readable —
# only reviews-admin.php (server-side filesystem) and the owner may see them.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>
